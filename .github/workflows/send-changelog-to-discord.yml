name: Notify Discord on Changelog Update

on:
  push:
    paths:
      - 'CHANGELOG.md'

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Get commit SHA
        id: get-sha
        run: echo "CURRENT_SHA=$(git rev-parse HEAD)" >> $GITHUB_ENV

      - name: Fetch previous commit
        id: fetch-prev
        run: |
          git fetch --depth=2
          echo "PREVIOUS_SHA=$(git rev-parse HEAD^)" >> $GITHUB_ENV

      - name: Get changed files
        id: get_changes
        run: |
          git diff --name-only ${{ env.PREVIOUS_SHA }} ${{ env.CURRENT_SHA }} > changes.txt
          cat changes.txt
          CHANGED_FILES=$(cat changes.txt)
          echo "CHANGED_FILES=$CHANGED_FILES" >> $GITHUB_ENV

      - name: Check if CHANGELOG.md was updated
        id: check_changelog
        run: |
          if echo "${{ env.CHANGED_FILES }}" | grep -q 'CHANGELOG.md'; then
            echo "CHANGELOG_MD_UPDATED=true" >> $GITHUB_ENV
          else
            echo "CHANGELOG_MD_UPDATED=false" >> $GITHUB_ENV
          fi

      - name: Extract latest update from CHANGELOG.md
        if: ${{ env.CHANGELOG_MD_UPDATED == 'true' }}
        id: extract_update
        run: |
          # Extract the latest update section from CHANGELOG.md
          awk '/^## \[/{i++}i==1' CHANGELOG.md > latest_update.txt
          # Limit to the first 2000 characters
          head -c 2000 latest_update.txt > latest_update.txt

      - name: Create JSON payload
        if: ${{ env.CHANGELOG_MD_UPDATED == 'true' }}
        id: create_json
        run: |
          # Create a JSON payload with the extracted update
          CONTENT=$(cat latest_update.txt | sed 's/"/\\"/g')
          PAYLOAD=$(jq -n --arg content "$CONTENT" '{content: $content}')
          echo "$PAYLOAD" > payload.json
          cat payload.json  # Debugging: Print payload to check its content

      - name: Validate JSON
        if: ${{ env.CHANGELOG_MD_UPDATED == 'true' }}
        id: validate_json
        run: |
          # Validate the JSON file
          cat payload.json | jq empty

      - name: Send update to Discord
        if: ${{ env.CHANGELOG_MD_UPDATED == 'true' }}
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
        run: |
          # Check if the payload is empty before sending
          if [ -s payload.json ]; then
            curl -H "Content-Type: application/json" -X POST -d @payload.json $DISCORD_WEBHOOK
          else
            echo "Payload is empty. Not sending to Discord."
            exit 1
          fi
